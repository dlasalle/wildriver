dependencies:
  pre:
    - ./configure --test && make
test:
  override:
    - make test
deployment:
  package:
    tag: /^v.*$/
    commands:
      - sudo apt-get install python3-pip 
      - pip3 install packagecore
      - packagecore -o "${CIRCLE_ARTIFACTS}" "${CIRCLE_TAG}"
