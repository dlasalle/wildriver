dependencies:
  pre:
    - ./configure --test && make
test:
  override:
    - make test
deployment:
  package:
    tag: /^v.*$/
    commands:
      - pyenv install 3.6.0
      - pip3 install packagecore
      - packagecore -o "${CIRCLE_ARTIFACTS}" "${CIRCLE_TAG}"
